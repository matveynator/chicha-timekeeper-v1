# SERVER SETTINGS
APP_ANTENNA_LISTENER_IP=0.0.0.0:4000
API_SERVER_LISTENER_IP=0.0.0.0:8000
TIME_ZONE="Europe/Moscow"

#race timeout in seconds after this time if no new tags received we consider race finished race_id=race_id+1
RACE_TIMEOUT_SEC=300

#default DB_TYPE = sqlite (saved to local file chicha.sqlite)
#sqlite not working on ARM/ARM64 - requires CGO compilation (CRAP!)
DB_TYPE=sqlite

# DATABASE
#DB_TYPE=postgres
#DB_HOST=localhost
#DB_NAME=chicha
#DB_PORT=5432
#DB_USER=postgres
#DB_PASSWORD=PaSSwOrD

# MISC SETTINGS
MINIMAL_LAP_TIME=30 #Duration in seconds, when data from RFID will be ignored
LAPS_SAVE_INTERVAL=1 #Interval in seconds, when laps buffer will be saved to Database

#PROXY SETTINGS (copy all incoming data to this address)
PROXY_ACTIVE=false
PROXY_HOST=127.0.0.1
PROXY_PORT=5000

#Administrator settings
ADMIN_LOGIN="admin"
